#include<stdio.h>
int a();
int main(void)
{
    int count,sum,max;
    count=0;
    sum=0;
    printf("Please input the max:");
    scanf("%d",&max);
    while(count++< max)
        sum = sum + a(count);
    printf("sum = %d\n",sum);
    return(0); 
}
  int a(x)
{
    return x*x;
}
