#include <stdio.h>
#define SIZE 8

int main(void)
{
    int ch[SIZE];
    int j=0,k;
    k=1;
    for(ch[0]=2;k<=SIZE-1;k++)
        ch[k]=2*ch[k-1];
    do{
        printf("%d\n",ch[j]);
        j++;
    }while((j<=(SIZE-1))==1);
    return 0;
        
    
    
}
