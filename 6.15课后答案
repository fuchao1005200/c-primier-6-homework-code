#include <stdio.h>
#include <string.h>
#define SIZE 20
int main(void)
{
    char a[255];
    int i;
    for(i=0;i<SIZE;i++)
        scanf("%c",&a[i]);
    for(i=SIZE-1;i>=0;i--)
        printf("%c",a[i]);
    printf("\n");
   
    printf("%c",a[0]);
    return 0;
}

