#include <stdio.h>

int main()
{
    char ch;
    int i;
    while((ch=getchar()) != '#')
    {
        printf("  %c=%5d",ch,ch);
        i++;
        if(i%8 == 0)
        {
            printf("\n");
        }
    }
    return 0;
}
